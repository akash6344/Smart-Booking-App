# Supabase configuration
# Obtain from Supabase Dashboard: Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service role key for server-side operations (keep secret, never expose to client)
# Obtain from Supabase Dashboard: Project Settings -> API
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# App URL for OAuth redirect (required for production)
# Local: http://localhost:3000
# Vercel: https://your-app.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase Postgres connection string (for migrations only)
# Dashboard -> Project Settings -> Database -> Connection string (URI)
# Use Transaction pooler and replace [YOUR-PASSWORD] with your database password
DATABASE_URL=postgresql://postgres.[project-ref]:[YOUR-PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres
